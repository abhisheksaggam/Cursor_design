# Figma: default source for variables is fixtures/figma/bridge-collections.json (export from Figma Bridge plugin).
# Optional PAT for file access / other features. Not required for variables compare when using the bridge file.
FIGMA_ACCESS_TOKEN=
FIGMA_FILE_KEY=zFN780oP27DS6zOAhdRSU7
# Optional; the app can derive the file key from this link if FIGMA_FILE_KEY is empty.
FIGMA_FILE_URL=https://www.figma.com/design/zFN780oP27DS6zOAhdRSU7/Cursor?node-id=1-777
# Set to 1 to call Figma REST /variables/local instead of the bridge file (PAT must include file_variables:read).
# FIGMA_USE_LIVE_VARIABLES_API=
GITHUB_TOKEN=
GITHUB_OWNER=abhisheksaggam
GITHUB_REPO=Cursor_design
GITHUB_TOKEN_FILE_PATH=tokens/source/tokens.json
